# 4.2 Security Hardware Management
* **Secure Boot:** Uses digital signatures to ensure only trusted software boots the OS.
* **Measured Boot:** Logs every step of the boot process for remote verification.
* **TPM (Trusted Platform Module):** A hardware chip that stores cryptographic keys and enables Full Disk Encryption (FDE).
